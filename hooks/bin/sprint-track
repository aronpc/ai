#!/bin/bash
# Hook: Abrir tracking.md para atalização
# Uso: git sprint-track ou manualmente

SPRINTS_TRACKING="sprints/tracking.md"

if [ -f "$SPRINTS_TRACKING" ]; then
    ${EDITOR:-code} "$SPRINTS_TRACKING"
else
    echo "Arquivo $SPRINTS_TRACKING não encontrado!"
    exit 1
fi
